version: "3.9"

services:
  frontend:
    build: .
    container_name: worksphere-frontend
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    networks:
      - worksphere-net

networks:
  worksphere-net:
    external: true